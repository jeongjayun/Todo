<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.eco.todo.mapper.UserMapper">

<!-- 회원가입 -->
	<insert id="join">
		INSERT INTO USERS
					( USER_ID
					, USER_PW
					, USER_NM
					)
			 VALUES ( #{user_id}
			 		, #{user_pw}
			 		, #{user_nm}
			   		)
	</insert>

</mapper>