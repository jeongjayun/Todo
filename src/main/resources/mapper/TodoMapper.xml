<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.eco.todo.todos.TodoMapper">
<!-- view  중복문 -->
	<sql id="selectView">
		SELECT  TODO_IDX          -- 투두 인덱스 --
				, USER_ID					-- 사용자 아이디 --
				, TODO_TITLE			-- 투두 제목 --
				, TODO_MEMO				-- 투두 메모 --
				, TODO_DDLN				-- 투두 마감기한 --
				, CREATED_TIME		-- 생성 일시 --	
				, UPDATED_TIME		-- 수정 일시 --
				, DELETED_TIME		-- 삭제 일시 --
				, FIL_TDY					-- 하루 여부 --
				, FIL_IMP					-- 중요 여부 --
				, FIL_CMPLT				-- 완료 여부 --
				, FIL_DLT					-- 삭제 여부 --
		FROM V_TODO_FILTER	
	</sql>
	
<!-- ASC,DESC 중복문 -->
	<sql id="ViewSettings">
	 	<trim prefix="ORDER BY" suffixOverrides=",">
				<if test="impTodo_top == '0'">fil_imp ASC,</if>
				<if test="impTodo_top != '0'">fil_imp DESC,</if>
				<if test="newTodo_top == '0'">CREATED_TIME ASC,</if>
				<if test="newTodo_top != '0'">CREATED_TIME DESC,</if>
		</trim>
	</sql>


<!-- 투두 작성 -->
	<insert id="saveTodo">
		<selectKey keyProperty="todo_idx" resultType="int" order="BEFORE">
			SELECT TODO_SEQ.nextval FROM DUAL
		</selectKey>
		INSERT ALL
				  	 INTO TODO
							   ( TODO_IDX
							   , USER_ID
							   , TODO_TITLE
							   , TODO_DDLN
							   , CREATED_TIME
							   )
						VALUES ( #{todo_idx}
							   , #{user_id}
							   , #{todo_title}
							   , #{todo_ddln, jdbcType=DATE}
							   , #{created_time}
							   )
				   INTO FILTERS
				   			   ( TODO_IDX
				   			   , USER_ID
				   			   , FIL_TDY
				   			   , FIL_IMP
				   			   , FIL_CMPLT
				   			   , FIL_DLT
				   			   )
				   	 	VALUES (
				   	 			 #{todo_idx}
				   	 		   , #{user_id}
								<if test="fil_tdy != null and fil_tdy != ''">, #{fil_tdy}</if>
								<if test="fil_tdy == null or fil_tdy == ''">, 0</if>
								<if test="fil_imp != null and fil_imp != ''">, #{fil_imp}</if>
								<if test="fil_imp == null or fil_imp == ''">, 0</if>
								<if test="fil_cmplt != null and fil_cmplt != ''">, #{fil_cmplt}</if>
								<if test="fil_cmplt == null or fil_cmplt == ''">, 0</if>
				   	 		   , 0
				   	 		   )
				   	 	SELECT * FROM DUAL   				   	 		   
	</insert>
	
<!-- 투두 삭제 -->
	<update id="deleteTodo">
		UPDATE FILTERS
		   SET FIL_DLT = '1'
		 WHERE TODO_IDX = #{todo_idx}
	</update>
	
	<update id="deleteTodoTime">
		UPDATE TODO
		   SET DELETED_TIME = #{deleted_time}
		 WHERE TODO_IDX = #{todo_idx}
	</update>
	
<!-- 투두 수정 -->
	<update id="updateTodo">
		UPDATE TODO
		 <trim prefix="SET" suffixOverrides=",">
	        <if test="todo_title != null and todo_title != ''">TODO_TITLE = #{todo_title},</if>
	        <if test="todo_memo != null and todo_memo != ''">TODO_MEMO = #{todo_memo},</if>
	        UPDATED_TIME = #{updated_time},
	        TODO_DDLN = #{todo_ddln, jdbcType=DATE}
	     </trim>
		 WHERE TODO_IDX = #{todo_idx}
		   AND USER_ID = #{user_id}
	</update>
	
<!-- 투두 필터 수정  -->
	<update id="updateFilters">
	    UPDATE FILTERS
	    <trim prefix="SET" suffixOverrides=",">
	        <if test="fil_tdy != null and fil_tdy != ''">FIL_TDY = #{fil_tdy},</if>
	        <if test="fil_imp != null and fil_imp != ''">FIL_IMP = #{fil_imp},</if>
	        <if test="fil_cmplt != null and fil_cmplt != ''">FIL_CMPLT = #{fil_cmplt},</if>
	        <if test="fil_dlt != null and fil_dlt != ''">FIL_DLT = #{fil_dlt},</if>
	    </trim>
	    WHERE TODO_IDX = #{todo_idx}  
	</update>
	
	<update id="updateTodoTime">
		UPDATE TODO
		   SET UPDATED_TIME = #{updated_time}
		 WHERE TODO_IDX = #{todo_idx}
	</update>

<!-- 선택한 할 일 상세보기 -->
	<select id="getTodoDetail">
		<include refid="selectView"></include>
		 WHERE USER_ID = #{user_id} 
		   AND TODO_IDX = #{todo_idx}
	</select>

<!-- 투두 검색 -->
	<select id="search">
		<include refid="selectView"></include>
			WHERE USER_ID = #{user_id}
			  AND TODO_TITLE LIKE '%'||#{todo_title}||'%'
			ORDER BY CREATED_TIME ASC
	</select>
	
</mapper>